# Set to 1 so the chat composer calls Flask `POST /api/eva/chat` (Vite proxies `/api` → http://127.0.0.1:5000).
# Requires: backend running (`cd backend && flask run --port 5000`) and `npm run dev` from this folder.
VITE_EVA_USE_BACKEND=0
